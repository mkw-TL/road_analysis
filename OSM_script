/*
This has been generated by the overpass-turbo wizard.
The original search was:
“highway=residential or service in Lexington”
*/
[out:json][timeout:25];
// fetch area “Blacksburg” to search in
{{geocodeArea:Blacksburg,Va}}->.searchArea;
// gather results
(
  // query part for: “highway=residential”
  way[highway=residential](area.searchArea);
  way[highway=motorway_link](area.searchArea);
  way[highway=trunk](area.searchArea);
  way[highway=trunk_link](area.searchArea);
  way[highway=primary](area.searchArea);
  way[highway=secondary](area.searchArea);
  way[highway=tertiary](area.searchArea);
  way[highway=unclassified](area.searchArea);
  way[highway=residential](area.searchArea);
  node[highway=motorway](area.searchArea);
  node[highway=motorway_link](area.searchArea);
  node[highway=trunk](area.searchArea);
  node[highway=trunk_link](area.searchArea);
  node[highway=primary](area.searchArea);
  node[highway=secondary](area.searchArea);
  node[highway=tertiary](area.searchArea);
  node[highway=unclassified](area.searchArea);
  node[highway=residential](area.searchArea);

);
// print results
out center;
(._;>;);
out skel tags meta qt;


  //node["highway"="residential" or "service"](area.searchArea);
  //way["highway"="residential"](area.searchArea);
  //relation["highway"="residential"](area.searchArea);